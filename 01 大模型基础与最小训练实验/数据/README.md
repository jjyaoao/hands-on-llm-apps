# 微型语言模型语料

课程自编的 24 段教学短文，20 段训练、4 段验证；不是互联网预训练语料。文本有意重复句式，便于 CPU 小模型学到局部规律。验证文档不与训练文档完全重复，但共享句式，所以验证 Loss 只能反映这一小型同分布实验，不能推断通用语言能力。

拆分在 corpus.json 的 split 中固定。字符词表只从训练文档建立，验证中的新字符映射为 `<UNK>`。窗口在各文档内部滑动，不跨文档。重叠窗口适合演示，但不同窗口并非独立样本。学生不应把窗口数当作独立文档数。
