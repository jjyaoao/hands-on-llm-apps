# 01 大模型基础与最小训练实验

从 Token、概率与 Loss 进入因果 Transformer，再训练微型语言模型并观察生成。

1. 先读 `课件` 中的 PDF。
2. 按文件名前的数字依次打开 `Notebook`，查看保存输出，再自行运行与修改。
3. `代码` 放可复用的 Python 源码与自检；`数据` 放本节输入。请保留整节目录。

01C 的训练使用教学用小语料，结果不能代表通用大模型能力。
