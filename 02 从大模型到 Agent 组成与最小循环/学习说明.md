# 02 从大模型到 Agent 组成与最小循环

观察 Agent 的组成、工具调用循环、错误恢复，以及可选的 DeepSeek 真实请求。

1. 先读 `课件` 中的 PDF。
2. 按文件名前的数字依次打开 `Notebook`，查看保存输出，再自行运行与修改。
3. `代码` 放可复用的 Python 源码与自检；`数据` 放本节输入。请保留整节目录。

02C 默认关闭真实请求；只有主动设置密钥并打开 RUN_LIVE 才会调用模型。
